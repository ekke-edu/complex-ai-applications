# 2. Vector RAG & Databases

Modern LLMs hallucinate and lack private knowledge. The industry standard solution is **Retrieval-Augmented Generation (RAG)**.

### Embeddings & Vector DBs
We convert text into numerical arrays (embeddings). The vector database (`ChromaDB`) finds the closest points in space rapidly.
