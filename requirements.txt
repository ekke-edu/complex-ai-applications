# === WEB BACKEND & API ===
fastapi>=0.115.0         # Modern, gyors webes keretrendszer az API végpontok megírásához
uvicorn>=0.30.0          # Villámgyors ASGI szerver a FastAPI alkalmazás futtatásához
pydantic>=2.8.0          # Adatvalidációs és típusellenőrző könyvtár (a FastAPI alapja)
tenacity>=0.20.0          # Aszinkron retry mechanizmus a külső API hívásokhoz

# === AI & VEKTOR ADATBÁZIS ===
google-genai>=0.1.0      # A hivatalos, modern SDK a Google Gemini modellek kezeléséhez
chromadb>=0.5.0          # Vektor-adatbázis dokumentumok tárolásához és RAG-alapú kereséshez

# === UI & KOMMUNIKÁCIÓ ===
streamlit>=1.37.0        # Gyors frontend keretrendszer az MI alkalmazás webes felületéhez
requests>=2.32.0         # Klasszikus HTTP könyvtár külső weboldalak és API-k meghívásához

# === KONFIGURÁCIÓ ===
python-dotenv>=1.0.1     # Környezeti változók (.env fájl) kezelése az API kulcsok védelmében

# === ADATBÁZIS (NoSQL) ===
motor>=3.4.0             # Aszinkron MongoDB driver a chathistory tárolásához

# === GRÁFADATBÁZIS (Neo4j) ===
neo4j>=5.23.1             # Hivatalos driver a GraphRAG relációk kezeléséhez